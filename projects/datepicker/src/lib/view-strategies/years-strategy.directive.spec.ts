import { YearsStrategyDirective } from './years-strategy.directive';

describe('YearsStrategyDirective', () => {
  it('should create an instance', () => {
    const directive = new YearsStrategyDirective();
    expect(directive).toBeTruthy();
  });
});
