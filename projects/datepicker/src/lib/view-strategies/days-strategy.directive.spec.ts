import { DaysStrategyDirective } from './days-strategy.directive';

describe('DaysStrategyDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysStrategyDirective();
    expect(directive).toBeTruthy();
  });
});
