import { MonthsStrategyDirective } from './months-strategy.directive';

describe('MonthsStrategyDirective', () => {
  it('should create an instance', () => {
    const directive = new MonthsStrategyDirective();
    expect(directive).toBeTruthy();
  });
});
